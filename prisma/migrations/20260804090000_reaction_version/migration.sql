-- KAN-52: 리액션 집계에 단조 버전을 붙인다.
--
-- 델타는 절대 count라 중복 배달에는 면역이지만 역순 배달에는 아니었다 — 늦게 도착한 옛
-- 절대값이 화면을 과거로 되돌린 채 굳었다. 토글마다 이 카운터를 잠그고 +1 해서 델타에
-- 실으면 번호 순서가 곧 커밋 순서이고, 수신 측이 낮은 번호를 버릴 수 있다.
--
-- 기본값 0으로 시작해도 안전하다: 기존 행의 첫 토글이 1을 받고, 화면이 들고 있는 기준선도
-- 조회 시점의 0이라 그 델타가 정상적으로 통과한다.
ALTER TABLE "ChatMessage" ADD COLUMN "reactionSeq" INTEGER NOT NULL DEFAULT 0;
